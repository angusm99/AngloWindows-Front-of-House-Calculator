# Imports

This folder stores external project material that has been pulled into the workspace for reference or porting.

## Current contents

- `manus-latest/`
  Latest extracted Manus ZIP provided by the user.

The active local app is still the FastAPI implementation in this repo, but the imported Manus source is kept here so UI behavior, docs, and logic can be ported forward in a controlled way.
